import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    gradientBg: {
        position: 'absolute',
        width: '100%',
        height: '100%',
    },
});

export default styles;

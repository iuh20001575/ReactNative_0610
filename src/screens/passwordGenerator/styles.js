const { StyleSheet } = require('react-native');

const styles = StyleSheet.create({
    flex1: {
        flex: 1,
    },
    container: {
        padding: 19,
        paddingBottom: 16,
    },
});

export default styles;
